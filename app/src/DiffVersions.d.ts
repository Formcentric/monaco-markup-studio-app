import React from 'react';
type DiffVersionsProps = {
    originalVersionNumber: number;
    checkedOut: boolean;
};
export declare const DiffVersions: ({ originalVersionNumber, checkedOut }: DiffVersionsProps) => React.JSX.Element;
export {};
